#ifndef DIRECTION_H
#define DIRECTION_H

/**
 * @brief
 * Ogólnodostępny typ wyliczeniowy,
 * który odpowiada za przechowanie kierunku
 * (bądź jego braku, jeśli trzeba).
 */
enum DIRECTION {
    NONE = 0,
    UP = 1,
    LEFT = 2,
    DOWN = 3,
    RIGHT = 4
};

#endif
